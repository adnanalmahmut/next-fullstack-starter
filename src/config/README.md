# Configuration

Validated application configuration and environment-variable schemas live here.

Only server configuration may access secrets. Public configuration must use explicitly approved `NEXT_PUBLIC_` variables.
