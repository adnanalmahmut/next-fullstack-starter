# Internationalization

Locale configuration, locale-aware navigation, direction mapping, and formatting utilities live here.

User-facing messages will be maintained in the root `messages` directory when next-intl is configured.
